---
layout: post
title:  "Noise Scheduler - Intuitive Approach"
date:   2026-06-14
categories:
---
<script defer src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml.js"></script>


Have you ever been in a hilly area or a valley and watched the fog slowly appear and spread? It thickens, reducing visibility, and over time, the scenic view dissolves into whiteness. In the same way, a Noise Scheduler does this to an image over time. As the number of time steps increases, the variance of the Gaussian noise increases. Eventually, the image distribution converges toward a standard Gaussian distribution ($$N(0,I)$$). 
<div class="center-gif">
    <img src="{{ '/assets/gifs/fog.gif' | relative_url }}"
         alt="Fog analogy"
         style="width:100%; max-height:300px; object-fit:cover;">
</div>
<figcaption style="font-size:0.9em; color:#666;">
  <strong>Figure 1.</strong> Fog gradually obscuring a landscape, analogous to the forward diffusion process.Source: Generated by Gemini
</figcaption>

## Why do we need a noise scheduler? Why can’t we just randomly add noise to the image and teach the diffusion model to predict?

To answer these questions, we should understand what happens in the diffusion models. This can be explained in two steps:

- Forward Diffusion: A noise scheduler progressively adds Gaussian noise to clean image over a series of timesteps. During training, the diffusion model is given noisy image and learns to predict the noise that was added. It starts with a clean image and ends with a heavily corrupted image that is effectively pure noise.

- Reverse Diffusion: Generation begins with a sample of pure Gaussian Noise. The diffusion model predicts the noise present at each timestep and this predicted noise is removed to obtain a slightly less noisy image. This process is repeated iteratively across timestpes, gradually transforming the nosiy sample into a clean, realistic image.

Therefore, for a diffusion model to predict the added noise effectively at every timestep, the noise scheduler must be carefully designed. the scheduler determines how much Gaussian Noise is added at each step of the forward diffusion process. 
If the scheduler is purely designed, some timesteps may be difficult or redundant making the reverse diffusion process less effective and ultimately degrading the quality of generated images.

To conclude, the scheduler is trying to balance two competing goals:
- Add noise slowly enough that neighboring timesteps remain predictable and the denoising task is learnable.
- Add noise fast enough that the forward process eventually transforms the data distribution into simple Gaussian distribution.

# What happens if noise is added slowly: 
The consecutive timesteps becomes nearly identical. As a result, model repeatedely learns similar denoising operations across many timesteps, leading to redundant training. This can make the diffusion process inefficient and may require a larger number of timesteps to reach pure noise.


# What happens if noise is added aggressively:
If noise is added too aggressively, the image becomes heavily corrupted within a few timesteps. 

Using the fog analogy, this is equivalent to asking an artist to reconstruct a scenic landscape that is already covered by a dense whiteout fog. Since very little information about the original scene remains visible, the reconstruction task becomes significantly more difficult.  In the same way, the model is forced to learn large denoising transitions instead of gradual corrections, making the reverse diffusion process more challenging and often leading to poorer generation quality.

A well-designed noise scheduler therefore introduces noise gradually, ensuring that the image information is removed progressively throughout the forward process. This creates meaningful intermediate states that the model can learn to denoise effectively, and enabling a stable reverse diffusion process for image generation.

## Types of Noise Schedulers

The noise schedulers can be broadly classified::
- Discrete noise schedulers: Time steps are discrete values,  $$ t \in [0, T] $$
- Continuous noise schedulers: Time steps are continous values, $$ t \in [0, 1] $$

Let us understand two popular discrete noise scheduler:
- Linear Noise Scheduler
- Cosine Noise Scheduler

## Linear Noise Scheduler

The Linear Noise Scheduler was introduced in the seminal paper [Denoising Diffusion Probabilistic Models](https://arxiv.org/abs/2006.11239). It specifies a linearly increasing variance schedule, where the amount of noise added at each timestep grows according to a linear function. The scheduler is defined as follows:

$$
\beta_t = \beta_{\text{start}}
+ \frac{t}{T-1}
(\beta_{\text{end}}-\beta_{\text{start}})
$$

The noise variance increases linearly from $$\beta_{\text{start}}$$ to $$\beta_{\text{end}}$$ over $$T$$ timesteps.

$$
\beta_t = \beta_{\text{start}} +
\frac{t}{T-1}
\left(\beta_{\text{end}} - \beta_{\text{start}}\right)
$$

The corresponding signal retention factor is:

$$
\alpha_t = 1 - \beta_t
$$

The cumulative signal preservation is:

$$
\bar{\alpha}_t = \prod_{i=1}^{t} \alpha_i
$$

## Cosine Scheduler 
The authors of [Improved Denoising Diffusion Probabilistic Models](https://arxiv.org/abs/2102.09672) observed that the linear schedule worked well for higher resolution images. However, it was suboptimal for images of resolution 64x64 and 32x32. 

Their analysis showed that the linear scheduler destroys image information too quickly during the later stages of the forward process. As a result, many of the time steps contains almost pure noise and contribute little to the learning process or the final image quality.


<figure style="text-align:center; margin:10px 0;">
    <img src="{{'/assets/images/linear_cosine.png' | relative_url }}"
         alt="Linear and Cosine schedules"
         style="width:100%; height:auto;" />

    <figcaption style="font-size:0.9em; color:#666; margin-top:6px;">
        <strong>Figure 2.</strong>
        Linear (top) and Cosine (bottom) schedules respectively at linearly spaced values of
        <em>t</em> from 0 to <em>T</em>. Source:
        <a href="https://arxiv.org/abs/2102.09672">
            Improved Denoising Diffusion Probabilistic Models
        </a>.
    </figcaption>
</figure>

To address this issue, the proposed Cosine noise scheduler was proposed. It introduces noise more grdually near the begining and end of the timeline while maintaining a relatively steady rate of information loss in the middle timesteps.
Instead of defining the noise variance directly, it specifies the cumulative signal preservation term i.e. $$\bar{\alpha}_t$$:

$$
\bar{\alpha}_t =
\frac{
\cos^2\!\left(
\frac{t/T + s}{1+s}
\frac{\pi}{2}
\right)
}{
\cos^2\!\left(
\frac{s}{1+s}
\frac{\pi}{2}
\right)
}
$$

where $$s = 0.008$$ is a small offset parameter.

The noise variance at timestep $$t$$ is then computed as:

$$
\beta_t
=
1 -
\frac{\bar{\alpha}_t}
{\bar{\alpha}_{t-1}}
$$

In the next post, we will explore the tools and metrics used to understand signal preservation and noise injection throughout the diffusion process.


Found a typo or technical inaccuracy? Please open an issue or submit a pull request on [GitHub]({{ site.github_repo}}).

If you found this post helpful, consider giving the repository a ⭐.